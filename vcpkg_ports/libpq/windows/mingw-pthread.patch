diff --git a/src/interfaces/libpq/meson.build b/src/interfaces/libpq/meson.build
index a74e885..c2b1e3f 100644
--- a/src/interfaces/libpq/meson.build
+++ b/src/interfaces/libpq/meson.build
@@ -19,12 +19,17 @@
 )
 libpq_so_sources = [] # for shared lib, in addition to the above
 
 if host_system == 'windows'
-  libpq_sources += files('pthread-win32.c', 'win32.c')
+  # MinGW toolchains (GCC, clang-based llvm-mingw) ship pthreads natively via
+  # -lpthread (winpthreads); bundling pg's pthread-win32.c shim causes duplicate
+  # symbol errors at link time. Only MSVC/clang-cl need the shim.
+  if cc.get_argument_syntax() == 'msvc'
+    libpq_sources += files('pthread-win32.c')
+  endif
+  libpq_sources += files('win32.c')
   libpq_so_sources += rc_lib_gen.process(win32ver_rc, extra_args: [
     '--NAME', 'libpq',
     '--FILEDESC', 'PostgreSQL Access Library',])
 endif
 
 if ssl.found()
   libpq_sources += files('fe-secure-common.c')
