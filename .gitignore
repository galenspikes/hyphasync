build
.idea
cmake-build-debug
duckdb_unittest_tempdir/
.DS_Store
testext
test/python/__pycache__/
.Rhistory
.cache/
# Sample databases / benchmark output for local testing (not committed).
# Create testdata/ locally (the download scripts do this) or symlink it to your
# own external storage — never commit the contents.
/testdata/
